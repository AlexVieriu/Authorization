@page "/roleAuthorization"

@attribute [Authorize(Roles = "HR")]

@inject NavigationManager navManager


@code
{
    string? exception = "";

    protected override void OnInitialized()
    {
        navManager.NavigateTo("/roleAuth", true);
    }
}
